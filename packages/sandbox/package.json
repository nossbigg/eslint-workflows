{
  "name": "sandbox",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "lint": "eslint --max-warnings=0 'src/**/*.js'",
    "lint:json": "yarn lint --format=json-with-metadata --output-file='./eslint-workflows/eslint-output.json'",
    "ew": "eslint-workflows"
  },
  "devDependencies": {
    "eslint": "^8.12.0",
    "@nossbigg/eslint-workflows": "0.0.0"
  }
}
